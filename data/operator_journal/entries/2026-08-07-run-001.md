# Run 001 — 2026-08-07

**Operator:** autonomous (claude-opus-4-8[1m]), remote agent
**Branch:** `operator/2026-08-07`
**Status:** Bootstrap run — established the operator journal and closed the single most
important measurement gap (SPY benchmark).

---

## 0. Situation on arrival

The scheduled prompt told me to read `MANDATE.md`, `LATEST.md`, `POST_MORTEM_RRS.md`, and
the 3 most recent journal entries — **none of these existed** anywhere in the repo or its
git history. This is genuinely the first operator run. So this run is a bootstrap: stand up
the journal infrastructure and do an honest baseline assessment, rather than "follow the
Protocol section of a MANDATE that isn't there."

I authored `MANDATE.md` from the safety constraints in the (authorized) scheduled prompt,
and wrote `POST_MORTEM_RRS.md` from the evidence actually in the repo. These are marked as
bootstrapped; a human can amend them.

## 1. Assessment — is this bot profitable? (honest answer: no evidence it is)

Surveyed all profitability artifacts (details in `POST_MORTEM_RRS.md`). Summary:

| Question | Answer from the artifacts |
|---|---|
| Best backtested return | ~6.8% / yr, Sharpe **0.11**, PF 1.29, 38.1% win, 215 trades (`data/optimization/optimization_2025-12-29.json`) |
| Costs modeled? | **No.** No commissions; slippage was "documented," never applied to prices |
| Beats SPY buy-and-hold? | **Never computed.** SPY ~10%/yr historically → best config likely *underperforms* passive |
| Live/paper track record | **2 outcomes total** (1 win, 1 loss); ~1,986 signals all with `outcome=None` |
| ML edge | CV AUC **0.54** (~coin flip), train AUC 0.99 (overfit) |
| "100X" docs | Aspiration; `DEPLOYMENT_SUMMARY.md` cites 50.2% win rate vs actual 38.1% |

**Verdict:** There is no credible evidence this system beats SPY buy-and-hold net of costs.
The best honest number available underperforms a passive hold. This is not yet a wind-down
call — the measurement itself was too broken to trust — but the burden of proof is now
explicit and high.

## 2. Decision

Per the mandate ("when evidence is thin, invest in measurement before strategy"), the
highest-leverage safe change this run is to **make the backtest tell the truth about SPY.**
No parameter tuning, no strategy changes — those would be premature and unmeasurable.

Hypothesis being tested going forward: *some RRS configuration beats SPY buy-and-hold net of
honest costs.* This run makes that hypothesis **falsifiable** by adding the benchmark; it
does not yet answer it (couldn't — see Verify).

## 3. What I changed

- **New:** `data/operator_journal/MANDATE.md` — operator constitution + protocol.
- **New:** `POST_MORTEM_RRS.md` — honest reconstruction of the bot's state and root causes.
- **New:** `data/operator_journal/entries/2026-08-07-run-001.md` (this file) + `LATEST.md`.
- **Code:** `scripts/run_backtest.py` — added `compute_spy_benchmark()` and integrated it:
  - New "BENCHMARK: SPY BUY & HOLD" report section computing per-config **alpha vs SPY**
    (using slippage-adjusted returns) and an explicit "NONE beat SPY" warning.
  - Saved-results JSON now includes `spy_buy_hold_return[_pct]` and per-config
    `slippage_adjusted_return_pct`, `alpha_vs_spy_pct`, `beats_spy`.
  - The function is **fail-open** (returns `(0.0, 0.0)` on any error), so it can never crash
    an otherwise-successful backtest, and handles tz-aware indices + Timestamp/date args.

Deliberately did **not** touch: `risk/` (untouched), engine internals, live/broker config,
`main.py`, or any strategy parameters.

## 4. Verification

- `py_compile` on `scripts/run_backtest.py`: **PASS**.
- Unit-tested `compute_spy_benchmark` logic in isolation with synthetic pandas frames:
  full-window (8.00%), sub-window (3.96%), tz-aware index, Timestamp args, and all
  fail-open edges (None / missing Close / single row / empty window→fallback) — **all correct**.
- **UNVERIFIED (could not run end-to-end):** the full backtest does not run in this remote
  environment — `yfinance` is proxy-blocked (SSL reset) and the app dependency tree
  (loguru, etc.) isn't installed. The benchmark math is validated in isolation; the human
  should run `python scripts/run_backtest.py` locally to produce the first real
  strategy-vs-SPY number. That number is the whole point — please run it.

## 5. Risk directory

**Not touched.** (`risk/` untouched this run.)

## 6. Recommendation for Run 002

1. **Run the updated `run_backtest.py` locally** (or via IBKR MCP data) and record the first
   honest strategy-vs-SPY alpha in the next journal entry. This is the gating datum.
2. **Propagate the benchmark to `run_walkforward_v2.py`** (the more rigorous 2-yr / out-of-
   sample test) — same pattern, so future comparisons are always benchmark-relative.
3. **Model real costs in the engine**, not just in reporting (per-share IBKR commission +
   applied slippage on entry/exit fills). Until then, treat all returns as optimistic.
4. **Investigate the broken outcome-tracking loop** (1,986 signals, 0 outcomes). A working
   feedback loop is likely worth more than any parameter change.
5. If, after benchmark + costs + out-of-sample, no config beats SPY: begin drafting the
   wind-down / repurpose recommendation per MANDATE §Mission.
