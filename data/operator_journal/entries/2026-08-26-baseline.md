# Operator Entry — 2026-08-26 — Baseline & Journal Bootstrap

**Operator:** autonomous run #1 (stateless)
**Branch:** `claude/adoring-feynman-7wwoko` (harness-pinned; see MANDATE §3)
**Disposition:** Establish honest baseline; bootstrap missing constitution; make
the measurement honest. No strategy/parameter change.

---

## 0. Situation on arrival

The scheduled task told me to read `MANDATE.md`, `LATEST.md`, and
`POST_MORTEM_RRS.md` first. **None of them existed** — nor did the
`data/operator_journal/` tree. Git history confirms they were never committed.
This is genuinely the first operator run; the constitution had never been
bootstrapped. The repo has been dormant since the last commit on 2026-03-06.

## 1. Assessment (evidence gathered this session)

- **Live account:** IBKR MCP `get_account_summary` → net-liq **$5**, buying
  power $5; `get_account_positions` → **empty**. This is NOT the $25K paper
  account (DUP995654) referenced in CLAUDE.md. There is no extractable live
  track record. (Open question for next run: which account is the real one?)
- **Benchmark:** IBKR `get_price_history` SPY, trailing 2yr monthly:
  $563.68 → $765.58 = **+35.8% (~16.5% annualized).**
- **Strategy evidence (from repo):** CLAUDE.md's own 2yr walk-forward best
  config = **+6.9% / 2yr (~3.4%/yr), GROSS.**
- **Cost modeling:** grep across `scripts/run_walkforward*.py`,
  `backtesting/engine*.py`, `research/backtest_harness.py` → **zero** commission
  / slippage / spread modeling anywhere. All reported returns are gross.
- **Signal plumbing:** `signal_metrics.json` shows 119 short / 1 long and only
  **2 recorded outcomes** ever (1 win, 1 loss); raw `signal_history.json` shows
  1,687 long / 299 short. The two disagree — measurement plumbing is suspect.
- **Data access:** Yahoo/yfinance is blocked by the agent proxy
  (`Connection reset`). IBKR MCP is the viable market-data source in this env.

## 2. Decision

Two things, both chosen to be honest and non-overfitting:

1. **Bootstrap the operator constitution** (`MANDATE.md`, `POST_MORTEM_RRS.md`,
   `entries/`, `LATEST.md`) so future stateless runs are grounded. Required by
   the task and simply missing.
2. **Make the measurement honest before touching the strategy.** Added
   `backtesting/costs.py` — a dependency-free commission + slippage + spread
   model — plus `tests/unit/test_costs.py`. I deliberately did NOT tweak any
   scanner parameter: with gross-only backtests and no live record, any
   parameter change would be curve-fitting to an inflated number. Fix the ruler
   first.

## 3. Changes made

- `data/operator_journal/MANDATE.md` — constitution (mission, hard constraints,
  protocol, standing facts, wind-down criteria).
- `POST_MORTEM_RRS.md` — honest history of how the bot reached this state.
- `backtesting/costs.py` — `CostModel` (per-fill commission w/ $1 min & 1% cap;
  slippage + half-spread in bps), `net_pnl`, `apply_costs`, `summarize`, and a
  `__main__` illustration.
- `tests/unit/test_costs.py` — 9 unit tests covering the math and edge cases.
- `data/operator_journal/entries/2026-08-26-baseline.md` + `LATEST.md`.

## 4. Verification

- **Cost model logic: PASS.** All 9 assertions verified via direct module load
  (`importlib`). The full `pytest` suite could not *collect* in this bare cloud
  checkout because the repo's root/`backtesting` `__init__` chain imports heavy
  deps (`config`, `loguru`, brokers) not installed here — an environment
  limitation, not a defect in the test. The test file is written against the
  production import path `from backtesting.costs import ...` and will pass in a
  normal dev/CI env once repo deps are installed.
- **Honest-cost illustration (`python3 backtesting/costs.py`):**
  ~$4.50/round-trip × 279 trades ≈ **$1,256 cost**; gross $1,716 →
  **net ~$460.50 = 1.84%/2yr (~0.9%/yr).**
- **Could NOT verify:** any live/end-to-end backtest run (yfinance blocked, repo
  deps absent). Not attempted rather than faked.

## 5. Headline finding

> The active RDT strategy's *best* honestly-adjusted estimate (~0.9%/yr net) is
> roughly **1/18th of SPY buy-and-hold (~16.5%/yr)** over the same window, with
> no live track record to show otherwise. As it stands, the bot does not beat —
> or come close to — buying and holding the index.

This is not yet a wind-down recommendation: it is run #1, and no honest
net-of-cost, out-of-sample search for an edge has actually been done (the tools
to do it honestly only exist as of today). But the burden of proof now sits
squarely on the active strategy.

## 6. Recommended next actions (for run #2)

1. **Resolve the account question** — determine whether a real $25K paper
   account exists and reconnect the operator's view to it; the $5 MCP account is
   uninformative.
2. **Wire `backtesting/costs.py` into the walk-forward harnesses** so the
   headline table is restated NET. (Left un-wired this run: the 1,119-line
   script can't be validated here without market data, and a blind edit risks
   silent breakage. Do it where it can be run.)
3. **Run one honest, net-of-cost, walk-forward test** using IBKR
   `get_price_history` as the data source (yfinance is blocked). If it clears
   SPY net of costs on out-of-sample windows, pursue it. If not, begin the
   wind-down conversation with the human per MANDATE §6.
4. **Investigate the 119-short/1-long vs 1,687-long/299-short discrepancy** —
   the signal accounting must be trustworthy before any signal-based claim is.

## 7. Constraints check

- PAPER only; `AUTO_TRADE` untouched; no broker credentials touched. ✅
- `risk/` directory: not modified. ✅
- Journal entry committed + `LATEST.md` updated. ✅ (this file)
- No merge to main; branch pushed for human review. ✅
